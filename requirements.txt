# App packages (Streamlit Cloud installs this file).
# Pinned to the versions the app was tested with, so a new release can't break the live app.
numpy==2.5.3
pandas==3.0.6
pyarrow==25.0.1
yfinance==1.7.0
streamlit==1.64.0
plotly==7.1.0
